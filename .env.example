# Slack App Configuration
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-token
SLACK_SIGNING_SECRET=your-signing-secret

# Claude Code Configuration
# The bot invokes the `claude` CLI — authenticate with `claude login` first.
# Path to claude binary (defaults to 'claude' on PATH)
# CLAUDE_EXECUTABLE=/Users/username/.local/bin/claude
# Model override (defaults to claude-code's built-in default; set to switch models)
# CLAUDE_MODEL=claude-opus-5

# Working Directory Configuration
# Base directory for relative paths (e.g., /Users/username/projects/)
# If set, you can use "cwd project-name" instead of full paths
BASE_DIRECTORY=/Users/username/projects/
# Default project — determines which CLAUDE.md, plugins, and settings are loaded
DEFAULT_WORKING_DIRECTORY=your-project

# Optional: API key auth (alternative to `claude login`)
# ANTHROPIC_API_KEY=your-anthropic-api-key
# CLAUDE_CODE_USE_BEDROCK=1
# CLAUDE_CODE_USE_VERTEX=1

# Webterm-driven claude runtime (claw-op2n Phase 2 demoable).
# If WEBTERM_CHANNELS is set, messages from those Slack channels are routed
# through a persistent interactive `claude` process hosted in webterm
# (http://127.0.0.1:7681) instead of the per-message SDK path. One webterm
# session is created per Slack thread. Sessions are in-memory only and are
# lost on bot restart (persistence is tracked under claw-usdo).
# Requires webterm running locally on the configured URL.
# WEBTERM_CHANNELS=C0XXXXXXXXX,C0YYYYYYYYY
# WEBTERM_URL=http://127.0.0.1:7681
# WEBTERM_CWD=/Users/username/projects/claudeclaw
# WEBTERM_CLAUDE_CMD=claude --dangerously-skip-permissions

# Development
# DEBUG=true
